# =============================================================================
# StillThere — Python Dependencies
# =============================================================================

# --- Core Framework --------------------------------------------------------
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Data Validation -------------------------------------------------------
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# --- Database --------------------------------------------------------------
sqlalchemy==2.0.31
asyncpg==0.29.0          # async PostgreSQL driver
psycopg2-binary==2.9.9   # sync driver (used by Alembic)
alembic==1.13.2

# --- Task Queue ------------------------------------------------------------
celery==5.4.0
redis==5.0.7
flower==2.0.1            # Celery monitoring UI

# --- HTTP / Web Scraping ---------------------------------------------------
httpx==0.27.0            # async HTTP client
beautifulsoup4==4.12.3   # HTML parsing
lxml==5.2.2              # fast XML/HTML parser for BS4

# --- AI --------------------------------------------------------------------
anthropic==0.28.1        # Claude API client

# --- Security / Middleware -------------------------------------------------
slowapi==0.1.9           # rate limiting for FastAPI
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4   # password hashing
bcrypt==3.2.2            # pin to 3.x — passlib 1.7.4 incompatible with bcrypt>=4

# --- Logging ---------------------------------------------------------------
structlog==24.2.0

# --- Utilities -------------------------------------------------------------
python-dotenv==1.0.1
aiofiles==23.2.1
tenacity==8.5.0          # retry logic for external API calls

# --- Testing ---------------------------------------------------------------
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0
factory-boy==3.3.0
respx==0.21.1            # mock httpx requests in tests
